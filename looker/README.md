# looker/

Looker Studio dashboard exports and data source configuration snapshots.

Store exported dashboard JSON here so the dashboard layout and calculated fields
can be version-controlled and re-imported after environment changes.

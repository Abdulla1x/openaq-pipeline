ruff==0.6.9
pytest==8.3.3
responses==0.25.3
sqlfluff==3.1.1
# sqlfluff requires chardet unpinned; >=6 falls outside the range requests
# 2.31.0 supports, which makes every test run emit a RequestsDependencyWarning.
# Dev-only pin — the Airflow image resolves this under constraints-2.9.1.
chardet==5.2.0
